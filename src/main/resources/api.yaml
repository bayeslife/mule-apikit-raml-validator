#%RAML 0.8
title: sample api
/currentuser/{uriparam}:
    put:
        body:
            application/json:
                schema: !jackson bayeslife.User
            text/xml:
                schema: !jaxb bayeslife.User
        responses:
            204: ~

